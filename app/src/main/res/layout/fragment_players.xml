<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".pages.home.players.PlayersBaseFragment"
    android:layout_marginTop="16dp"
    android:layout_marginLeft="16dp"
    android:layout_marginRight="16dp"
    android:orientation="vertical">

    <LinearLayout
        android:id="@+id/top_bar_players_page"
        android:layout_width="match_parent"
        android:layout_height="50sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/players_international_button"
            android:layout_height="match_parent"
            android:layout_width="fill_parent"
            android:fontFamily="@font/asap_bold"
            android:textAllCaps="false"
            android:backgroundTint="@color/caramel"
            app:strokeColor="@color/black"
            android:textColor="@color/black"
            app:strokeWidth="1dp"
            app:shapeAppearanceOverlay="@style/PillShapeAppearance"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:layout_marginStart="5dp"
            android:layout_weight="0"
            android:textSize="14sp"

            android:text="@string/default_player_picker_string" />

    </LinearLayout>

    <FrameLayout
        android:layout_marginTop="16dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    <include
        layout="@layout/loading_error_states"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/players_fragment_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:listitem="@layout/players_icon_item" />

    </FrameLayout>







</LinearLayout>